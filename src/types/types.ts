export interface Cocktail {
  src: string;
  title: string;
  description: string;
}

export interface Drink {
  src: string;
  title: string;
  description: string;
}

export interface NavItem {
  label: string;
  href: string;
  translationKey: string;
}
